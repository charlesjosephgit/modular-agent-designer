# TODO

- Add a dedicated safe-expression language for workflow routing if eval usage grows beyond simple conditions.
- Add richer dry-run output that estimates provider/API-key requirements per node.
- Add package publishing checks that verify examples and documentation links before release.
